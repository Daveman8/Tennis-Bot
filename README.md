# Tennis-Bot

How to make changes
1. Clone the repo "git clone (repo link)"
2. Navigate to repo on IDE "cd (Name of foler/file)
3. Make your own separate branch "git checkout -b (Whatever you want the branch name as)"
4. commit "git add ." and "git commit -m (What was changed)"
5. push "git push origin (name of branch)"
